# 工具与速记

## Git
- 分支：`git checkout -b feature/x`
- 变基：`git rebase -i HEAD~3`
- Worktree：`git worktree add ../path branch`

## Nano
- 保存：`Ctrl+O`
- 退出：`Ctrl+X`
- 搜索：`Ctrl+W`

## Rust 语法
- 所有权/借用：`&T` / `&mut T`
- Result：`Result<T, E>` + `?` 运算符
- Option：`Option<T>` + `if let`
