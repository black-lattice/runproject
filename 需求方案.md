# 需求方案：微前端（micro-app）接入公司平台

## 背景与目标
- 目标：在当前 RunProject 桌面应用中，以微前端方式挂载公司平台作为子应用。
- 诉求：子应用独立运行，但可按需访问主应用状态或权限信息。
- 环境：公司平台在内网可访问，通过 VPN 也可访问。

## 现状与约束
- 公司平台为单体应用，尚未按 micro-app 规范改造。
- 路由模式为 history。
- 当前主应用基于 Tauri + Vite + React，使用 HashRouter。
- 需考虑 Tauri WebView 的安全策略与资源加载限制。

## 接入思路（主应用侧）
1) 引入并初始化 micro-app
   - 在应用入口初始化 micro-app。
   - 统一注册子应用名称与基础配置。
2) 新增子应用承载页面
   - 新建页面用于渲染 `<micro-app>` 组件。
   - 保证高度/滚动适配现有布局（TitleBar + TabBar）。
3) 接入路由与导航
   - 在路由配置中加入子应用入口。
   - TabBar 中新增可关闭的“公司平台”页签。
4) 主子应用通信
   - 通过 micro-app 的数据通信机制传递权限、用户态或配置信息。
   - 若涉及敏感信息，建议仅下发短期票据或已脱敏的数据。

## 多子应用并存方案（多页切换）
- 每个子应用使用独立 `name`、`baseroute` 与 `url`，避免路由与资源冲突。
- 主应用路由中为每个子应用创建独立入口页面，按 Tab 切换打开。
- 主应用维护子应用清单配置，统一管理加载与权限下发。

## 主应用改造清单（详细）
1) 依赖与初始化
   - 引入并初始化 micro-app（仅初始化一次）。
   - 示例（在主应用入口初始化）：
     ```jsx
     import microApp from '@micro-zoe/micro-app';

     microApp.start();
     ```
2) 子应用配置管理
   - 新增子应用配置清单，包含：`name`、`title`、`url`、`baseroute`、`authRequired`。
   - 示例（配置清单）：
     ```js
     export const MICRO_APPS = [
       {
         name: 'massage-web',
         title: '按按管理平台',
         url: 'https://intranet.example.com/massage-web',
         baseroute: '/massage-web',
         authRequired: true
       },
       {
         name: 'crm-web',
         title: '客户管理系统',
         url: 'https://intranet.example.com/crm-web',
         baseroute: '/crm-web',
         authRequired: true
       }
     ];
     ```
3) 子应用承载页面
   - 为每个子应用创建独立页面组件，渲染 `<micro-app>` 容器。
   - 处理高度/滚动/加载异常兜底。
   - 示例（承载页面）：
     ```jsx
     export default function MassageWebPage() {
       return (
         <div className="h-full w-full overflow-hidden">
           <micro-app
             name="massage-web"
             url="https://intranet.example.com/massage-web"
             baseroute="/massage-web"
           />
         </div>
       );
     }
     ```
4) 路由与 Tab
   - 在路由表中增加子应用入口。
   - TabBar 中支持打开/关闭子应用页签。
   - 示例（路由入口）：
     ```jsx
     <Route path="/massage-web" element={<MassageWebPage />} />
     <Route path="/crm-web" element={<CrmWebPage />} />
     ```
5) 权限与状态下发
   - 统一在主应用获取登录态/权限信息。
   - 按子应用配置下发最小必要数据（短期 token 优先）。
   - 示例（下发权限数据）：
     ```jsx
     <micro-app
       name="massage-web"
       url="https://intranet.example.com/massage-web"
       baseroute="/massage-web"
       data={{ token: 'access_token_xxx', userId: 'u123', roles: ['admin'] }}
     />
     ```
6) 统一异常与可用性提示
   - 子应用加载失败、网络不可达、鉴权失败的提示与重试入口。
   - 示例（加载失败提示容器）：
     ```jsx
     <div className="h-full w-full">
       <micro-app
         name="massage-web"
         url="https://intranet.example.com/massage-web"
         baseroute="/massage-web"
       />
       {/* 这里可结合全局错误状态显示兜底提示 */}
     </div>
     ```

## 子应用改造清单（详细）
1) micro-app 生命周期适配
   - 支持 mount/unmount，避免重复渲染与内存泄漏。
   - 示例（子应用入口）：
     ```jsx
     import ReactDOM from 'react-dom/client';
     import App from './App';

     let root;

     function render(container) {
       root = ReactDOM.createRoot(container);
       root.render(<App />);
     }

     if (window.__MICRO_APP__) {
       window['micro-app-massage-web'] = {
         mount: () => render(document.getElementById('root')),
         unmount: () => root?.unmount()
       };
     } else {
       render(document.getElementById('root'));
     }
     ```
2) history 路由基座
   - Router 设置 `basename`，与主应用 `baseroute` 保持一致。
   - 示例（React Router basename）：
     ```jsx
     import { BrowserRouter } from 'react-router-dom';

     const baseName = window.__MICRO_APP__ ? '/massage-web' : '/';

     export default function AppRouter() {
       return (
         <BrowserRouter basename={baseName}>
           {/* routes */}
         </BrowserRouter>
       );
     }
     ```
   - 示例（同时支持微前端与浏览器独立运行）：
     ```jsx
     import { BrowserRouter } from 'react-router-dom';

     const baseName = window.__MICRO_APP__
       ? '/massage-web'
       : import.meta.env.BASE_URL;

     export default function AppRouter() {
       return (
         <BrowserRouter basename={baseName}>
           {/* routes */}
         </BrowserRouter>
       );
     }
     ```
3) 资源路径配置
   - Vite 设置 `base`，确保静态资源在子路径可加载。
   - 示例（vite.config.js）：
     ```js
     export default defineConfig({
       base: '/massage-web/'
     });
     ```
4) 主子通信接入
   - 接收主应用下发的数据，支持订阅更新。
   - 将权限态与用户态写入统一状态模块或请求拦截器。
   - 示例（读取主应用数据）：
     ```js
     if (window.__MICRO_APP__) {
       const data = window.microApp.getData();
       // 使用 data.token / data.userId / data.roles
     }
     ```
5) 本地存储隔离
   - localStorage/sessionStorage key 加前缀，避免多子应用冲突。
   - 示例（前缀封装）：
     ```js
     const key = (k) => `massage-web:${k}`;
     localStorage.setItem(key('token'), 'access_token_xxx');
     ```

## 登录态共享方案（推荐优先级）
1) 同主域 Cookie + SSO
   - 子应用自动携带 Cookie，主应用仅负责引导与初始化。
   - 优点：安全与稳定性高，改造成本相对低。
   - 示例（子应用请求自动携带 Cookie）：
     ```js
     axios.defaults.withCredentials = true;
     ```
2) 主应用下发 token
   - 主应用通过 micro-app 数据通道下发 token/用户态。
   - 子应用请求拦截器注入 token，处理过期与刷新。
   - 示例（请求拦截器注入 token）：
     ```js
     axios.interceptors.request.use((config) => {
       const data = window.__MICRO_APP__ ? window.microApp.getData() : {};
       if (data?.token) config.headers.Authorization = `Bearer ${data.token}`;
       return config;
     });
     ```
3) 混合方案
   - Cookie 维持登录态，主应用下发租户/权限范围等辅助信息。
   - 示例（下发租户信息）：
     ```jsx
     <micro-app
       name="massage-web"
       url="https://intranet.example.com/massage-web"
       baseroute="/massage-web"
       data={{ tenantId: 't001', roles: ['admin'] }}
     />
     ```

## 配置示例（占位）
```json
[
  {
    "name": "app-a",
    "title": "子应用A",
    "url": "https://intranet.example.com/app-a",
    "baseroute": "/app-a",
    "authRequired": true
  },
  {
    "name": "app-b",
    "title": "子应用B",
    "url": "https://intranet.example.com/app-b",
    "baseroute": "/app-b",
    "authRequired": true
  }
]
```

## 接入思路（子应用侧）
1) 微前端改造
   - 按 micro-app 官方规范调整子应用入口与挂载逻辑。
2) history 路由兼容
   - 配置子应用 `publicPath` 与 `baseroute`，避免路由冲突。
   - 确保静态资源在子路径下可正确加载。
3) 鉴权与状态同步
   - 支持接收主应用下发的权限或用户态信息。
   - 需要定义数据结构与更新时机。

## 关键风险与限制
- history 路由在微前端场景下的 `baseroute` 处理与跳转回退风险。
- Tauri WebView 对跨域资源加载与安全策略的影响。
- 内网环境与 VPN 下的可达性、证书与跨域策略问题。
- 权限透传的安全边界与最小化暴露原则。
- 多子应用并存时本地存储与全局变量污染风险。

## 分阶段计划
1) 阶段一：可用性验证
   - 主应用接入 micro-app，创建子应用承载页与路由。
   - 使用公司平台现有入口进行最小化加载验证。
2) 阶段二：子应用改造
   - 按 micro-app 规范调整公司平台入口。
   - 处理 history 路由与静态资源路径问题。
3) 阶段三：权限与状态接入
   - 设计主子通信数据结构与同步策略。
   - 实现权限信息下发与子应用消费。
4) 阶段四：稳定性与安全
   - 完善错误边界、加载失败兜底与网络异常提示。
   - 校验权限暴露范围与安全策略。

## 实施优先级（细化）
1) 优先级 P0：主应用接入 micro-app + 单个子应用验证
2) 优先级 P1：history 路由与资源路径完整适配
3) 优先级 P1：权限下发与子应用消费链路打通
4) 优先级 P2：多子应用并行配置与 Tab 交互完善
5) 优先级 P2：异常兜底、监控与诊断增强

## 验收标准
- 主应用可稳定加载公司平台子应用页面。
- 子应用路由跳转、刷新与回退行为正常。
- 子应用能读取主应用下发的权限或用户态信息。
- VPN/内网环境下稳定可访问，出现异常有明确提示。
- 多子应用之间互不影响，资源与存储隔离有效。

## 待确认事项
- 公司平台改造范围与负责人安排。
- 权限/用户态数据的传递方式与字段定义。
- 子应用部署地址与访问控制策略。
- 子应用技术栈与入口文件位置（便于改造）。
