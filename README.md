# RunProject

## 📖 项目能力概述

RunProject 是一个面向多项目开发的桌面工作台，能力聚焦在脚本执行、环境切换与终端工作流的统一管理。

## ✨ 当前核心特性

- **工作区管理**：一键扫描目录，自动识别 npm/yarn/pnpm 项目。
- **项目详情刷新**：支持刷新工作区，也支持在右侧项目详情卡片单独刷新当前项目，重新读取 `package.json` 的名称、scripts、Node 版本与包管理器信息。
- **智能脚本解析**：自动解析 `package.json` scripts，可视化点击运行。
- **环境隔离**：集成 NVM/FNM，支持为不同项目自动切换 Node.js 版本。
- **内置终端**：内置 PTY + xterm.js，支持会话重连、历史回放与多终端页签。
- **跨平台兼容**：Shell/路径/打开目录指令做了统一封装，Windows/macOS/Linux 可运行。
- **标签系统**：支持工作区/项目/命令标签，多标签输入与分类展示。
- **快速搜索**：支持关键词与 `#标签` 过滤，命令面板（Cmd/Ctrl+K）快速跳转与执行。
- **数据格式化**：支持 JSON、XML、SQL、CSS、HTML、JavaScript 等多种格式的美化。
- **现代化 UI**：基于 Shadcn/UI 设计，简洁美观，支持亮/暗模式。
- **Git 基础集成**：查看和切换分支。

## 🧩 典型使用场景

- 扫描一个工作区后，在左侧快速筛选项目，在右侧查看项目详情、脚本和 Git 分支。
- 修改某个项目的 `package.json` 后，直接点击右侧“刷新”按钮，立即同步最新 scripts 和项目信息。
