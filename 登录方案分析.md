# 项目登录方案分析

## 1. 登录架构概述

本项目采用了**双认证机制**的登录方式：
- **JWT Token 认证**：用于API请求的权限验证
- **CAS单点登录**：用于统一身份认证管理

## 2. 核心实现文件

### 2.1 登录页面组件 

```tsx
// 登录方法
onLogin = () => {
  this.formRef.current?.validateFields().then((values) => {
    accountLoginApi(values).then((result) => {
      if (result?.token) {
        saveJWTToken(result?.token)
      }
      // 登陆成功后缓存 token，并进行跳转
      const { redirectUrl } = this.props.match.query
      location.replace(redirectUrl || process.env.routerBase)
    })
  })
}
```

**实现说明**：
- 使用 Ant Design 的 Form 组件实现登录表单
- 通过 `accountLoginApi` 调用登录接口
- 登录成功后保存 JWT token 并跳转页面

### 2.2 JWT Token 管理 

```tsx
import { getCasToken } from '@hd/cas-login'
import { JWT_LOCAL_TOKEN } from '@/constants/common'

export const saveJWTToken = (token: string) => {
  localStorage.setItem(JWT_LOCAL_TOKEN, token)
}

export const getJWTToken = () => {
  return getCasToken()
}

export const delJWTToken = () => {
  localStorage.removeItem(JWT_LOCAL_TOKEN)
}
```

**实现说明**：
- 提供 token 的存储、获取和删除方法
- 关键：`getJWTToken` 方法实际调用的是 `@hd/cas-login` 库的 `getCasToken` 方法
- token 存储在 localStorage 中，键名为 `massage-web-JWT`

### 2.3 API 请求认证拦截器 

```tsx
// 头部header新增 token
axios.interceptors.request.use((config) => {
  if (!jwtToken) {
    jwtToken = getJWTToken()
  }
  if (jwtToken) {
    config.headers = {
      ...config.headers,
      Authorization: `Bearer ${jwtToken}`,
    }
  }
  // @ts-ignore
  config.headers['x-permission-tenant'] = 72

  return config
})

// 响应拦截器处理认证错误
axios.interceptors.response.use(undefined, interceptors.axios)
```

**实现说明**：
- 请求拦截器：自动为每个请求添加 Authorization header
- 响应拦截器：使用 `@hd/cas-login` 库的拦截器处理认证相关错误
- 项目还使用了请求签名机制增强安全性

### 2.4 登录 API 接口 

```tsx
// 登录内部用户账号
export const accountLoginApi = axios.post('/api/v1/employ/login')

// 获取账号相关用户信息
export const getUserInfoApi = axios.get(
  '/api/v1/employee-service/employ/find-by-userId',
)
```

**实现说明**：
- 登录接口：`/api/v1/employ/login`
- 获取用户信息接口：`/api/v1/employee-service/employ/find-by-userId`

### 2.5 全局状态管理 

```tsx
// 登录
login() {
    console.log('登录')
},
// 登出
logout() {
    // 清理实名认证倒计时数据
    delAuthCoverTime()
    casLogout()
},
// 获取用户信息
async getUserInfo() {
    const userInfo = await getUserInfoApi()
    dispatch.global.setUserInfo(userInfo)
}
```

**实现说明**：
- 登出时调用 `casLogout()` 方法，清理 CAS 登录状态
- 提供获取用户信息的方法

## 3. 登录流程

1. **用户访问登录页面**：输入邮箱和密码
2. **前端发送登录请求**：调用 `accountLoginApi` 接口
3. **后端验证身份**：验证邮箱和密码，生成 JWT token
4. **前端存储 token**：调用 `saveJWTToken` 存储 token
5. **页面跳转**：跳转到首页或指定的重定向地址
6. **后续 API 请求**：自动通过 axios 拦截器添加 Authorization header
7. **认证状态维护**：使用 `@hd/cas-login` 库管理单点登录状态

## 4. 安全特性

1. **JWT Token 认证**：无状态认证，便于水平扩展
2. **请求签名**：使用 `@hd/request-sign` 库为请求添加签名
3. **CAS 单点登录**：统一身份认证管理
4. **跨域凭证携带**：通过 `withCredentials: true` 配置支持跨域请求携带凭证

## 5. 登出流程

1. **调用登出方法**：触发 `logout()` 方法
2. **清理本地数据**：调用 `delAuthCoverTime()` 清理认证相关数据
3. **CAS 登出**：调用 `casLogout()` 退出 CAS 登录状态
4. **重定向到登录页**：CAS 登出后会重定向到登录页面

## 6. 技术依赖

- **@hd/cas-login**：单点登录库，提供统一身份认证
- **JWT**：无状态认证机制
- **axios**：HTTP 请求库，通过拦截器实现认证
- **Ant Design**：UI 组件库，提供登录表单

## 7. 代码优化建议

1. **错误处理增强**：
   - 登录失败时添加更详细的错误提示
   - 考虑添加登录次数限制，防止暴力破解

2. **安全性提升**：
   - 考虑使用 HTTPS 传输敏感信息
   - 实现 token 过期自动刷新机制

3. **用户体验优化**：
   - 添加登录加载状态
   - 实现记住密码功能（注意安全存储）
   - 添加验证码机制，防止自动化攻击

4. **代码结构优化**：
   - 将登录相关逻辑抽取为独立的服务
   - 使用 React Hooks 重构登录组件，减少 class 组件的使用

## 8. 配置说明

### 8.1 axios 配置

```tsx
// 基础的axios配置
const basicConfig = {
  withCredentials: true,
}
```

**配置说明**：
- `withCredentials: true`：允许浏览器在跨域请求时携带凭证信息（如 cookies、HTTP 认证信息等）
- 这是实现 CAS 单点登录和跨域认证的必要配置

### 8.2 JWT Token 存储配置

```tsx
// JWT本地存储 key
export const JWT_LOCAL_TOKEN = 'massage-web-JWT'
```

**配置说明**：
- 定义了 JWT token 在 localStorage 中的存储键名

## 9. 总结

本项目采用了**JWT Token + CAS 单点登录**的混合认证方案，既保证了 API 请求的安全性，又实现了统一的身份认证管理。登录流程清晰，代码结构合理，同时通过请求签名等机制增强了系统安全性。

这种方案适合企业级应用，尤其是需要与多个系统集成的场景，能够提供一致的用户体验和安全的认证机制。